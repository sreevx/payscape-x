"""Rules layer — placeholder only.

Declarative, versioned business rules (SLA windows, thresholds, provider
behaviour) will live here. Rules consume structured events and feed the
engines. No rules are implemented in Part 1.
"""