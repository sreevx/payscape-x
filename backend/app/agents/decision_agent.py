"""Future: recommend actions for human approval."""

from app.agents.base import BaseAgent


class DecisionAgent(BaseAgent):
    """(Placeholder) Recommends actions that still require human approval."""

    name = "decision_agent"