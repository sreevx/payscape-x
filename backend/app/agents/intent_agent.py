"""Future: reconstruct the customer's intended business outcome."""

from app.agents.base import BaseAgent


class IntentAgent(BaseAgent):
    """(Placeholder) Determines what outcome a transaction was supposed to achieve."""

    name = "intent_agent"