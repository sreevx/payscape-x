"""Future: collect and validate structured events as evidence."""

from app.agents.base import BaseAgent


class EvidenceAgent(BaseAgent):
    """(Placeholder) Gathers structured events and checks their integrity."""

    name = "evidence_agent"